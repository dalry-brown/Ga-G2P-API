# PyTorch CPU version (smaller, faster install for deployment)
torch==2.1.0
--extra-index-url https://download.pytorch.org/whl/cpu

# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Core dependencies (only what G2P needs)
numpy>=1.24.0